# SHA-GCL Official Sources

Primary source:

- `Stacked Hybrid-Attention and Group Collaborative Learning for Unbiased Scene
  Graph Generation`, Dong et al., CVPR 2022.

Implementation interpretation:

- SHA-GCL combines stacked hybrid attention with group collaborative learning
  over predicate groups.
- OpenSGG validates PredCls first using GT boxes and object labels.

Checkpoint status:

- No local SHA-GCL checkpoint was found in `outputs/pretrained`.
- No paper-number alignment is claimed.
