# PENet Official Sources

Primary source:

- `Prototype-based Embedding Network for Scene Graph Generation`, Zheng et al.,
  CVPR 2023.

Implementation interpretation:

- PENet uses object semantic prototypes and visual-semantic fusion for relation
  prediction.
- OpenSGG validates PredCls first using GT boxes and object labels.

Checkpoint status:

- No local PENet checkpoint was found in `outputs/pretrained`.
- No paper-number alignment is claimed.
