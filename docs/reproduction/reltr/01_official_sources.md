# RelTR Official Sources

Primary source:

- `RelTR: Relation Transformer for Scene Graph Generation`, Cong et al.,
  ECCV 2022.

OpenSGG context:

- RelTR is implemented as a one-stage transformer-based SGG model.
- A local checkpoint is available at `outputs/pretrained/reltr/reltr_vg.pth`.

Paper-number alignment:

- Not claimed from this tiny CPU slice.
