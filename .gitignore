# data/
data/OpenImages/
data/VisualGenome/

# Local docs and plans. The authoritative tracked workflow lives in
# `reproduction/`; `docs/` is a local/historical archive.
docs/*

# Local agent/editor configuration
.claude/
CLAUDE.md
.vscode/
.idea/

# Skills installed locally via `npx skills add` (see skills-lock.json for the
# upstream source of each). The lockfile is local provenance only; it is not
# part of the reproduction evidence in `reproduction/`.
.agents/
skills-lock.json

ckpts/
weights/
checkpoints/
runs/
results/
logs/
wandb/
lightning_logs/
lib/fpn/box_intersections_cpu/

# ---- New unified outputs (replaces scattered old dirs) ----
outputs/runs/
outputs/analysis/
# outputs/reports/ holds the hand-written analysis reports and is tracked on
# purpose, unlike the generated artifacts under outputs/analysis/. Anything that
# is not a report (a stray figure, csv or json) is still ignored.
#
# One tracked file violates the rule below rather than proving it:
# outputs/reports/relational_emergence/v2/development_evidence.json is a
# generated artifact that was force-added. It is frozen by
# tools/reproduction/check_repository_boundary.py (rule R2) so it cannot change,
# and it leaves the repository with the rest of the research surface.
outputs/reports/**/*.csv
outputs/reports/**/*.json
outputs/reports/**/*.png
outputs/pretrained/**
outputs/pretrained/**/*.pth
outputs/pretrained/**/*.pt
outputs/pretrained/**/*.ckpt
outputs/pretrained/**/*.tar
outputs/pretrained/**/*.tar.gz
outputs/pretrained/**/*.zip
!outputs/pretrained/
!outputs/pretrained/README.md

# Model checkpoint directories may contain generated metrics/config sidecars.
**/checkpoints/
!tools/checkpoints/
!tools/checkpoints/**

__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

*.pth
*.pt
*.ckpt
# Array dumps are always generated, never authored: they belong in an artifact
# backend, with only the URI and its hash tracked.
*.npy
*.npz

# Local staging area for the research extraction (issue #113). The extracted
# repository is a git repository of its own, so it must never be committed here;
# this entry has to exist before the clone, or `git add -A` would record it as a
# gitlink.
.research-extraction/

.env
.venv/
*.zip
*.tar
*.tar.gz
core.*
.DS_Store
*.swp
*.swo

*.json.resume
.ddp_eval_cache/

# Build artifacts
*.ninja_*
*.so
*.cuda.o
*.cpu.o
build.ninja
.ninja_deps
.ninja_log

# Reports. Anchored to the repository root on purpose: an unanchored `reports/`
# also matches `outputs/reports/`, which the block above says is tracked.
/reports/
migration_report.md
